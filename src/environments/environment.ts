
/** we need build : ng build --configuration=production
 * we need to serve the configuration : ng serve --configuration=production
 * 
  */

export const environment = {
  production: true,
  apiUrl: 'http://localhost:5000/',
};
